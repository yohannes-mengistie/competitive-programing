class Solution(object):
    def targetIndices(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
        arranged = sorted(nums)
        res = []
        for i in range(len(arranged)):
            if arranged[i]==target:
                res.append(i)

        return res
